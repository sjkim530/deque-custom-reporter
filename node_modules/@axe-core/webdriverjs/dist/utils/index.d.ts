import type { ElementContext } from 'axe-core';
/**
 * Get running context
 * @param {Array} include
 * @param {Array} exclude
 * @returns {(ElementContext | null)}
 */
export declare const normalizeContext: (include: string[], exclude: string[]) => ElementContext | null;
